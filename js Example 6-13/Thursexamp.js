//Create an array that holds the number 1-100. Print out all numbers that are not divisible by 5 and are not.
let numbers = [];

// for(let i = 0; i < 100; i++){
//     numbers.push(i + 1);
//     if((numbers[i] % 5 !== 0) && (numbers[i] % 2 !== 0)){
//         console.log(numbers[i]);
//     }
// }

for(let i = 0; i < 100; i++){
    numbers.push(i + 1);
        if(numbers[i] % 5 !== 0)
            numbers.splice(i:1) {
        
        
                
console.log(numbers[i]);
}